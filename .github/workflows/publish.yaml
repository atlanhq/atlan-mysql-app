name: Publish

on:
  push:
    branches: [main]
    paths:
      - 'atlan.yaml'
      - '.github/workflows/publish.yaml'
  workflow_dispatch:
    inputs:
      ref:
        description: "Branch or SHA to build and publish (default: current ref)"
        required: false
        type: string
      channel:
        description: "Target channel (e.g. 'all' for draft release). Ignored when tenants is set."
        required: false
        type: string
        default: "all"
      tenants:
        description: "Comma-separated tenant IDs for targeted publish. Takes precedence over channel."
        required: false
        type: string

jobs:
  publish:
    uses: atlanhq/application-sdk/.github/workflows/publish-app.yaml@main
    with:
      ref: ${{ inputs.ref || github.ref }}
      channel: ${{ inputs.channel || 'all' }}
      tenants: ${{ inputs.tenants || '' }}
    secrets: inherit
