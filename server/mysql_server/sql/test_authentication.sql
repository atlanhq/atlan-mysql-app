/*
 * File: test_authentication.sql
 * Purpose: Simple query to verify database connectivity and authentication
 *
 * This query performs a minimal operation (SELECT 1) to test that:
 *   - Database connection is established
 *   - Authentication credentials are valid
 *   - User has basic query permissions
 *
 * Returns: Single row with value 1 on success
 */
SELECT 1;